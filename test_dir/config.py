"""Another test file."""

app_config = {
    "app_name": "my_app",
    "port": 8080
}

database_config = {
    "url": "postgresql://localhost/mydb",
    "pool_size": 10
}

# This should be ignored
_internal_config = {"secret": "key"}
